# 🚀 快速开始指南

## 第一步：安装依赖

```bash
cd Hot-Stock
pip install -r backend/requirements.txt
```

如果遇到安装问题，可以使用国内镜像源：
```bash
pip install -r backend/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 第二步：初始化数据库

```bash
python manage.py init
```

## 第三步：运行首次分析（可选但推荐）

```bash
python manage.py analyze
```

**注意**：首次运行需要下载大量数据，可能需要 5-10 分钟。你也可以跳过此步骤，稍后在 Web 界面点击"立即分析"按钮。

## 第四步：启动服务器

方式 1 - 使用管理脚本：
```bash
python manage.py server
```

方式 2 - 使用启动脚本（推荐）：
```bash
./run.sh
```

## 第五步：访问网站

打开浏览器访问：`http://localhost:8000`

## 💡 常用命令

```bash
# 查看统计信息
python manage.py stats

# 手动运行分析
python manage.py analyze

# 启动服务器
python manage.py server
```

## 🔧 可选配置

### 配置 NewsAPI Key（推荐）

1. 访问 https://newsapi.org/ 注册并获取免费 API key
2. 复制环境变量模板：
   ```bash
   cp .env.example .env
   ```
3. 编辑 `.env` 文件，填入你的 API key：
   ```
   NEWS_API_KEY=你的API密钥
   ```

**注意**：如果不配置 NewsAPI key，系统将使用模拟数据进行测试。

### 設定熱門股票來源

系統會自動從 Yahoo Finance 抓取前一交易日最活躍的美股（預設 100 檔）。可在 `config.py` 調整：

```python
HOT_STOCK_LIMIT = 100      # 要抓取的熱門美股數量
USE_DYNAMIC_SYMBOLS = True # 若想使用備援清單而非線上熱門榜，改為 False
FALLBACK_STOCK_SYMBOLS = [ # 外部來源不可用時的備援清單
    "AAPL", "MSFT", "GOOGL", "AMZN", "META", "TSLA",
]
```

### 调整评分权重

编辑 `config.py` 文件中的 `SCORING_WEIGHTS`：

```python
SCORING_WEIGHTS = {
    "price_change": 0.25,      # 价格变化权重
    "volume_ratio": 0.20,      # 成交量比率权重
    "news_count": 0.25,        # 新闻数量权重
    "news_sentiment": 0.15,    # 新闻情绪权重
    "trend_score": 0.15,       # 趋势分数权重
}
```

## 📱 使用技巧

1. **查看股票详情**：点击榜单中的"查看详情"按钮
2. **切换榜单**：点击顶部的标签页切换不同榜单
3. **查看历史数据**：使用日期选择器选择不同日期的数据
4. **手动触发分析**：点击"立即分析"按钮手动运行分析任务

## ❓ 常见问题

### Q: 首次运行很慢怎么办？
A: 首次运行需要下载大量股票和新闻数据，这是正常现象。可将 `HOT_STOCK_LIMIT` 调小，或暂时将 `USE_DYNAMIC_SYMBOLS` 设为 `False` 以缩短抓取时间。

### Q: 显示"无法加载数据"怎么办？
A: 请先运行一次分析任务：`python manage.py analyze`

### Q: 新闻数据获取失败？
A: 检查是否配置了 NewsAPI key。如果没有配置，系统会使用模拟数据。

### Q: 如何设置自动分析？
A: 系统已配置每日 UTC 16:30 自动运行分析（美股收盘后）。可在 `config.py` 中修改 `COLLECT_TIME`。

## 🎯 下一步

- 探索 API 文档：http://localhost:8000/docs
- 查看完整文档：README.md
- 自定义配置：config.py

祝使用愉快！📈
