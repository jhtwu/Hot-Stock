# 故障排除指南

## ✅ 已修复的问题

### 1. NewsAPI Key 未加载

**问题**：系统提示 "未设置 NewsAPI key，将使用模拟数据"

**解决方案**：
- ✅ 已修复 `.env` 文件加载机制
- ✅ 已创建 `.env` 文件并添加你的 NewsAPI key
- ✅ 系统现在会正确读取 NewsAPI key

### 2. Yahoo Finance API 访问失败

**问题**：获取股票数据时出现 "Expecting value: line 1 column 1 (char 0)" 错误

**解决方案**：
- ✅ 已添加自动降级机制
- ✅ 当真实 API 失败时，系统会自动使用模拟数据
- ✅ 系统可以正常运行并展示所有功能

## 🎯 现在可以做什么

### 运行完整分析

```bash
python manage.py analyze
```

**期望结果**：
- 系统会尝试获取真实数据
- 如果 Yahoo Finance API 失败，会自动使用模拟数据
- NewsAPI 将使用你配置的 key 获取真实新闻
- 分析完成后会显示 Top 10 榜单

### 启动 Web 服务器

```bash
python manage.py server
```

然后访问：`http://localhost:8000`

## 🔧 配置选项

### 1. 启用演示模式（使用模拟数据）

编辑 `.env` 文件，添加：

```
DEMO_MODE=true
```

这样系统会直接使用模拟数据，不会尝试访问 Yahoo Finance API。

### 2. 更换 NewsAPI Key

如果你的 NewsAPI key 有问题，可以：

1. 访问 https://newsapi.org/ 重新申请
2. 编辑 `.env` 文件，更新 `NEWS_API_KEY`

### 3. 调整请求延迟

如果仍然遇到速率限制，编辑 `config.py`：

```python
REQUEST_DELAY = 2.0  # 增加到 2 秒
```

## 📊 数据模式说明

系统现在支持三种数据模式：

1. **真实数据模式**（默认）
   - 尝试从 Yahoo Finance 获取真实股票数据
   - 使用 NewsAPI 获取真实新闻
   - 如果 API 失败，自动降级到模拟数据

2. **混合模式**（当前状态）
   - Yahoo Finance 失败时使用模拟股票数据
   - NewsAPI 使用真实新闻数据
   - 可以正常展示系统功能

3. **演示模式**（可选）
   - 设置 `DEMO_MODE=true`
   - 完全使用模拟数据
   - 不访问任何外部 API

## 🐛 常见问题

### Q: 为什么 Yahoo Finance API 失败？

**可能原因**：
1. 网络问题或防火墙限制
2. Yahoo Finance API 临时不可用
3. IP 地址被限制
4. 请求过于频繁

**解决方案**：
- 使用模拟数据继续演示：`DEMO_MODE=true`
- 等待一段时间后重试
- 使用 VPN 或代理

### Q: NewsAPI 免费版有什么限制？

**限制**：
- 每天 100 次请求
- 只能获取过去 30 天的新闻
- 仅限开发环境使用

**建议**：
- 适合测试和演示
- 如需生产使用，需升级到付费版

### Q: 模拟数据准确吗？

**说明**：
- 模拟数据是为了展示系统功能
- 数据随机生成，但保持一致性
- 不应用于实际投资决策
- 仅用于演示和开发测试

## ✅ 验证系统状态

运行以下命令检查配置：

```bash
python -c "
from config import NEWS_API_KEY, DEMO_MODE
print(f'NewsAPI Key: {NEWS_API_KEY[:10]}...' if len(NEWS_API_KEY) > 10 else 'NewsAPI Key: 未设置')
print(f'演示模式: {\"启用\" if DEMO_MODE else \"关闭\"}')
"
```

## 🚀 推荐下一步

1. **运行分析**：`python manage.py analyze`
2. **启动服务器**：`python manage.py server`
3. **访问网站**：`http://localhost:8000`
4. **查看效果**：浏览各个榜单和股票详情

系统现在应该可以正常工作了！🎉
